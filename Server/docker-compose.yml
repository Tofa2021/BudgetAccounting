version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres-hibernate
    environment:
      POSTGRES_DB: BudgetAccounting
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data: